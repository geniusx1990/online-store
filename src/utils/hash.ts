class Hash {
    
    addHash(arr: string[], filter: string) {
        // let url = window.location.href;
        // let urlHash;
        // if(!url.includes('#')) {
        //     urlHash = `#${filter}=${arr.join(',')}`;
        //     window.location.href = url + urlHash;
        // }

    }
}

export default Hash;
